// The whole site is static: every page is prerendered at build time and served
// from a CDN. No server, no runtime, nothing to keep warm.
export const prerender = true;
export const trailingSlash = 'never';
